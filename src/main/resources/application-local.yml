server:
  port: 5555

logging:
  level:
    root: INFO

spring:
  data:
    mongodb:
      authentication-database: ${LOCAL_AUTH_DB}
      database: ${LOCAL_PAYMENT_DB}
      username: ${LOCAL_DB_USER}
      password: ${LOCAL_DB_PWD}